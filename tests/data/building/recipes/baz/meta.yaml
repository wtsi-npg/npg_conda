{% set version = "1.0.0" %}

package:
  name: baz
  version: "{{ version }}"

about:
  home: https://github.com/wtsi-npg/npg_conda.git
  license: GPL3
  summary: "Baz dummy package"

build:
  number: 0

test:
  commands:
    - baz
